# Components

Reusable React UI components live here.

Keep route-specific page composition in `app/`. Move UI into this directory when it is shared across multiple routes or when it meaningfully improves readability.
