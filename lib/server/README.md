# Server Helpers

Server-only utilities live here.

Use this directory for helpers that must never run in the browser, such as privileged service calls, internal API helpers, or request-scoped server utilities.
